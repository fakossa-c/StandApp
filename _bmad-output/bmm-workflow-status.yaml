# BMM Workflow Status - StandApp
# Generated: 2025-12-29
# Track: BMad Method (Greenfield)

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#   - skipped: User chose not to include this workflow
#
# Completion Status:
#   - {file-path}: File created/found (replaces initial status)
#   - skipped: Optional/conditional workflow that was skipped

generated: "2025-12-29"
project: "StandApp"
project_type: "web-application"
selected_track: "bmad-method"
field_type: "greenfield"
workflow_path: "method-greenfield.yaml"

workflow_status:
  # Phase 0 - Discovery (Optional)
  phase_0_discovery:
    brainstorm-project:
      status: skipped
      agent: analyst
      command: brainstorm-project
    research:
      status: skipped
      agent: analyst
      command: research
    product-brief:
      status: "_bmad-output/planning-artifacts/product-brief-StandApp-2025-12-29.md"
      agent: analyst
      command: product-brief
      completed: "2025-12-29"
      note: "Completed - recommended input for PRD"

  # Phase 1 - Planning (Required)
  phase_1_planning:
    prd:
      status: required
      agent: pm
      command: prd
      output: "Product Requirements Document with FRs and NFRs"
    create-ux-design:
      status: conditional
      condition: if_has_ui
      agent: ux-designer
      command: create-ux-design
      note: "Determined after PRD - user/agent decides if needed"

  # Phase 2 - Solutioning (Required)
  phase_2_solutioning:
    create-architecture:
      status: required
      agent: architect
      command: create-architecture
      output: "System architecture document"
    create-epics-and-stories:
      status: required
      agent: pm
      command: create-epics-and-stories
      note: "Break down PRD into implementable epics and stories"
    test-design:
      status: recommended
      agent: tea
      command: test-design
      output: "System-level testability review"
    implementation-readiness:
      status: required
      agent: architect
      command: implementation-readiness
      note: "Validates PRD + Architecture + Epics + UX alignment"

  # Phase 3 - Implementation (Required)
  phase_3_implementation:
    sprint-planning:
      status: required
      agent: sm
      command: sprint-planning
      note: "Creates sprint plan - subsequent work tracked in sprint-status.yaml"
